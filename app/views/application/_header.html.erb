<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "QSL", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to "Manage Callsign", callsigns_path %></li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm navbar-btn navbar-right" %>
        <p class="navbar-text navbar-right navbar-user">Signed in as <%= current_user.email %> </p>
      <% else %>
        <div class="btn-group navbar-btn navbar-right">
          <%= link_to "Sign In", login_path, class: "btn btn-default btn-sm" %>
          <%= link_to "Register", register_path, class: "btn btn-primary btn-sm" %>
        </div>
      <% end %>
    </div>

  </div>
</nav>
